---
title: "Delete a Page — DELETE /v1/new-page/{id} | Pharen Hub"
sidebarTitle: "Delete Page"
description: "Permanently delete a page from your Pharen Hub workspace by ID. This action is irreversible — deleted pages cannot be recovered via the API."
---

The Delete Page endpoint permanently removes a page from your Pharen Hub workspace. Once deleted, the page and all its content are gone — there is no recycle bin or soft-delete behaviour accessible through the API. Use this endpoint with care, and consider confirming with your users before making irreversible deletions in client applications.

## Endpoint

```bash
DELETE https://api.pharen.de/v1/new-page/{id}
```

## Path Parameters

<ParamField path="id" type="string" required>
  The unique identifier of the page to delete. You can retrieve this from the [List Pages](/api-reference/pages/list) endpoint.
</ParamField>

## Request Example

<CodeGroup>

```bash curl
curl https://api.pharen.de/v1/new-page/page_01hx9k2m3nqvw4e5r6t7y8u9p \
  --request DELETE \
  --header "Authorization: Bearer YOUR_API_KEY"
```

```javascript JavaScript (fetch)
const pageId = "page_01hx9k2m3nqvw4e5r6t7y8u9p";

const response = await fetch(`https://api.pharen.de/v1/new-page/${pageId}`, {
  method: "DELETE",
  headers: {
    "Authorization": `Bearer ${process.env.PHAREN_API_KEY}`,
  },
});

if (response.status === 404) {
  throw new Error("Page not found — it may have already been deleted.");
}

if (!response.ok) {
  const error = await response.json();
  throw new Error(error.error.message);
}

// 204 No Content — no response body to parse
console.log("Page deleted successfully.");
```

```python Python (requests)
import os
import requests

page_id = "page_01hx9k2m3nqvw4e5r6t7y8u9p"

response = requests.delete(
    f"https://api.pharen.de/v1/new-page/{page_id}",
    headers={
        "Authorization": f"Bearer {os.environ['PHAREN_API_KEY']}",
    },
)

if response.status_code == 404:
    raise ValueError("Page not found — it may have already been deleted.")

response.raise_for_status()

# 204 No Content — nothing to deserialise
print("Page deleted successfully.")
```

</CodeGroup>

<Note>
  A successful delete request returns `204 No Content` with an empty response body — there is nothing to parse or deserialize. Make sure your HTTP client handles empty responses without throwing an error.
</Note>

## Response

A successful deletion returns `204 No Content` with no response body.

| Status | Body | Meaning |
|--------|------|---------|
| `204 No Content` | *(empty)* | The page was found and successfully deleted. |
| `401 Unauthorized` | Error object | Your API key is missing or invalid. |
| `403 Forbidden` | Error object | Your API key does not have permission to delete this page. |
| `404 Not Found` | Error object | No page with the given ID exists in this workspace. |

## Error Responses

**Page not found (`404`):**

```json
{
  "error": {
    "code": "not_found",
    "message": "No page with ID 'page_01hx9k2m3nqvw4e5r6t7y8u9p' exists in this workspace."
  }
}
```

**Insufficient permissions (`403`):**

```json
{
  "error": {
    "code": "insufficient_permissions",
    "message": "Your API key does not have permission to delete pages in this workspace."
  }
}
```

<Warning>
  Deletion is permanent and immediate. There is no confirmation step, no soft delete, and no API-level undo. If your integration deletes pages in bulk, build in safeguards — such as a dry-run mode that lists pages before deleting them — to prevent accidental data loss.
</Warning>
