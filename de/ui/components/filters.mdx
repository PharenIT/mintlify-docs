---
title: "Filters"
sidebarTitle: "Filters"
description: "Importiere und verwende die Filters-Komponenten aus @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Mit **Filters** kannst du mit strukturierten operativen Daten zu arbeiten. Diese Seite dokumentiert alle Vue-Komponenten, die das Modul öffentlich exportiert.

<PharenUiPreview name="filters" title="Filters Vorschau" />

<Accordion title="Code anzeigen">

```vue
<script setup lang="ts">
import { Filters } from '@pharen/ui';
import { ref } from 'vue';
import type { Filter, FilterFieldConfig } from '@pharen/ui';

const filters = ref<Filter[]>([]);
const fields: FilterFieldConfig[] = [
  { id: 'status', label: 'Status', type: 'select', options: [
    { label: 'Active', value: 'active' },
    { label: 'Paused', value: 'paused' },
  ] },
];
</script>

<template>
  <Filters v-model="filters" :fields="fields" />
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Importiere `@pharen/ui/styles.css` genau einmal im Einstiegspunkt deiner Anwendung.
</Note>

## Verwendung

```ts
import { Filters, FilterInput, FilterValueSelector } from '@pharen/ui';
```

## Exporte

| Export | Aufgabe |
| --- | --- |
| `Filters` | Exportierte Vue-Komponente dieses Moduls. |
| `FilterInput` | Exportierte Vue-Komponente dieses Moduls. |
| `FilterValueSelector` | Exportierte Vue-Komponente dieses Moduls. |

## Quellcode

[Quellcode auf GitHub öffnen](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/filters)
