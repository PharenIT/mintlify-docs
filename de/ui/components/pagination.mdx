---
title: "Pagination"
sidebarTitle: "Pagination"
description: "Importiere und verwende die Pagination-Komponenten aus @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Mit **Pagination** kannst du eine Aktion oder Navigation aufzubauen. Diese Seite dokumentiert alle Vue-Komponenten, die das Modul öffentlich exportiert.

<PharenUiPreview name="pagination" title="Pagination Vorschau" />

<Accordion title="Code anzeigen">

```vue
<script setup lang="ts">
import { Pagination, PaginationContent, PaginationEllipsis, PaginationItem, PaginationNext, PaginationPrevious } from '@pharen/ui';
</script>

<template>
  <Pagination v-slot="{ page }" :items-per-page="10" :total="100" :default-page="3">
    <PaginationContent v-slot="{ items }">
      <PaginationPrevious />
      <template v-for="(item, index) in items" :key="index">
        <PaginationItem v-if="item.type === 'page'" :value="item.value" :is-active="item.value === page">{{ item.value }}</PaginationItem>
        <PaginationEllipsis v-else :index="index" />
      </template>
      <PaginationNext />
    </PaginationContent>
  </Pagination>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Importiere `@pharen/ui/styles.css` genau einmal im Einstiegspunkt deiner Anwendung.
</Note>

## Verwendung

```ts
import { Pagination, PaginationContent, PaginationEllipsis, PaginationFirst, PaginationItem, PaginationLast, PaginationLink, PaginationNext, PaginationPrevious } from '@pharen/ui';
```

## Exporte

| Export | Aufgabe |
| --- | --- |
| `Pagination` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationContent` | Enthält den sichtbaren Inhalt. |
| `PaginationEllipsis` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationFirst` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationItem` | Stellt ein einzelnes Element dar. |
| `PaginationLast` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationLink` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationNext` | Exportierte Vue-Komponente dieses Moduls. |
| `PaginationPrevious` | Exportierte Vue-Komponente dieses Moduls. |

## Quellcode

[Quellcode auf GitHub öffnen](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/pagination)
