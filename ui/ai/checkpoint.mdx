---
title: "Checkpoint"
sidebarTitle: "Checkpoint"
description: "Import and use the Checkpoint components from @pharen/ui/components/ai."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Checkpoint** to build AI interfaces. This page covers every Vue component exported by the public module.

<PharenUiPreview name="checkpoint" title="Checkpoint preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { Checkpoint, CheckpointIcon, CheckpointTrigger } from '@pharen/ui/components/ai';
</script>

<template>
  <Checkpoint class="w-full max-w-lg">
    <CheckpointTrigger tooltip="Restore this checkpoint" class="flex items-center gap-2 rounded-md px-2 py-1 text-sm hover:bg-muted">
      <CheckpointIcon /> Saved before publishing
    </CheckpointTrigger>
  </Checkpoint>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { Checkpoint, CheckpointIcon, CheckpointTrigger } from '@pharen/ui/components/ai';
```

## Exports

| Export | Purpose |
| --- | --- |
| `Checkpoint` | Exported Vue component from this module. |
| `CheckpointIcon` | Exported Vue component from this module. |
| `CheckpointTrigger` | Opens or activates the component. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/ai/checkpoint)
