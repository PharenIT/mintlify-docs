---
title: "Hover card"
sidebarTitle: "Hover card"
description: "Import and use the Hover card components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Hover card** to show contextual content or user feedback. This page covers every Vue component exported by the public module.

<PharenUiPreview name="hover-card" title="Hover card preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { HoverCard, HoverCardTrigger, HoverCardContent } from '@pharen/ui';
</script>

<template>
  <HoverCard>
    <HoverCardTrigger>Open hover card</HoverCardTrigger>
    <HoverCardContent>Hover card content</HoverCardContent>
  </HoverCard>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { HoverCard, HoverCardTrigger, HoverCardContent } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `HoverCard` | Exported Vue component from this module. |
| `HoverCardTrigger` | Opens or activates the component. |
| `HoverCardContent` | Contains the visible content. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/hover-card)
