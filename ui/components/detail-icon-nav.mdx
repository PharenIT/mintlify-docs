---
title: "Detail icon nav"
sidebarTitle: "Detail icon nav"
description: "Import and use the Detail icon nav components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Detail icon nav** to build an action or navigation pattern. This page covers every Vue component exported by the public module.

<PharenUiPreview name="detail-icon-nav" title="Detail icon nav preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { DetailIconNav } from '@pharen/ui';

const active = ref('overview');
const items = [
  { id: 'overview', label: 'Overview', icon: 'lucide:layout-dashboard' },
  { id: 'activity', label: 'Activity', icon: 'lucide:activity', badge: '3' },
  { id: 'settings', label: 'Settings', icon: 'lucide:settings' },
];
</script>

<template>
  <DetailIconNav :items="items" :active-id="active" @select="active = $event" />
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { DetailIconNav } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `DetailIconNav` | Exported Vue component from this module. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/detail-icon-nav)
