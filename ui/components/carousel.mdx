---
title: "Carousel"
sidebarTitle: "Carousel"
description: "Import and use the Carousel components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Carousel** to organize content and responsive regions. This page covers every Vue component exported by the public module.

<PharenUiPreview name="carousel" title="Carousel preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { Carousel, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from '@pharen/ui';
</script>

<template>
  <Carousel class="w-full max-w-md px-10">
    <CarouselContent>
      <CarouselItem v-for="item in ['Workflows', 'Agents', 'Knowledge']" :key="item">
        <div class="grid h-32 place-items-center rounded-xl border bg-card text-lg font-semibold shadow-sm">{{ item }}</div>
      </CarouselItem>
    </CarouselContent>
    <CarouselPrevious class="left-0" />
    <CarouselNext class="right-0" />
  </Carousel>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { Carousel, CarouselContent, CarouselItem, CarouselNext, CarouselPrevious } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `Carousel` | Exported Vue component from this module. |
| `CarouselContent` | Contains the visible content. |
| `CarouselItem` | Represents one item. |
| `CarouselNext` | Exported Vue component from this module. |
| `CarouselPrevious` | Exported Vue component from this module. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/carousel)
