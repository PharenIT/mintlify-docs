---
title: "Resizable"
sidebarTitle: "Resizable"
description: "Import and use the Resizable components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Resizable** to organize content and responsive regions. This page covers every Vue component exported by the public module.

<PharenUiPreview name="resizable" title="Resizable preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { ResizableHandle, ResizablePanel, ResizablePanelGroup } from '@pharen/ui';
</script>

<template>
  <ResizablePanelGroup direction="horizontal" class="h-56 w-full max-w-lg overflow-hidden rounded-xl border">
    <ResizablePanel :default-size="35"><div class="grid size-full place-items-center bg-muted/40 text-sm font-medium">Sidebar</div></ResizablePanel>
    <ResizableHandle with-handle />
    <ResizablePanel :default-size="65"><div class="grid size-full place-items-center text-sm font-medium">Content</div></ResizablePanel>
  </ResizablePanelGroup>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { ResizableHandle, ResizablePanel, ResizablePanelGroup } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `ResizableHandle` | Exported Vue component from this module. |
| `ResizablePanel` | Exported Vue component from this module. |
| `ResizablePanelGroup` | Groups related items. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/resizable)
