---
title: "Breadcrumb"
sidebarTitle: "Breadcrumb"
description: "Import and use the Breadcrumb components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Breadcrumb** to show the current location in a hierarchy. This page covers every Vue component exported by the public module.

<PharenUiPreview name="breadcrumb" title="Breadcrumb preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { Breadcrumb, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from '@pharen/ui';
</script>

<template>
  <Breadcrumb>
    <BreadcrumbList>
      <BreadcrumbItem><BreadcrumbLink href="/">Home</BreadcrumbLink></BreadcrumbItem>
      <BreadcrumbSeparator />
      <BreadcrumbItem><BreadcrumbPage>Settings</BreadcrumbPage></BreadcrumbItem>
    </BreadcrumbList>
  </Breadcrumb>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { Breadcrumb, BreadcrumbEllipsis, BreadcrumbItem, BreadcrumbLink, BreadcrumbList, BreadcrumbPage, BreadcrumbSeparator } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `Breadcrumb` | Exported Vue component from this module. |
| `BreadcrumbEllipsis` | Exported Vue component from this module. |
| `BreadcrumbItem` | Represents one item. |
| `BreadcrumbLink` | Exported Vue component from this module. |
| `BreadcrumbList` | Exported Vue component from this module. |
| `BreadcrumbPage` | Exported Vue component from this module. |
| `BreadcrumbSeparator` | Separates adjacent regions. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/breadcrumb)
