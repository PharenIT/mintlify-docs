---
title: "Input group"
sidebarTitle: "Input group"
description: "Import and use the Input group components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Input group** to collect and edit a value. This page covers every Vue component exported by the public module.

<PharenUiPreview name="input-group" title="Input group preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText } from '@pharen/ui';
</script>

<template>
  <InputGroup class="w-full max-w-md">
    <InputGroupAddon><InputGroupText>https://</InputGroupText></InputGroupAddon>
    <InputGroupInput value="pharen.app/workflows" aria-label="Workspace URL" />
    <InputGroupAddon align="inline-end"><InputGroupButton>Copy</InputGroupButton></InputGroupAddon>
  </InputGroup>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { InputGroup, InputGroupAddon, InputGroupButton, InputGroupInput, InputGroupText, InputGroupTextarea } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `InputGroup` | Groups related items. |
| `InputGroupAddon` | Exported Vue component from this module. |
| `InputGroupButton` | Exported Vue component from this module. |
| `InputGroupInput` | Exported Vue component from this module. |
| `InputGroupText` | Exported Vue component from this module. |
| `InputGroupTextarea` | Exported Vue component from this module. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/input-group)
