---
title: "Number field"
sidebarTitle: "Number field"
description: "Import and use the Number field components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Number field** to collect and edit a value. This page covers every Vue component exported by the public module.

<PharenUiPreview name="number-field" title="Number field preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { NumberField, NumberFieldDecrement, NumberFieldGroup, NumberFieldIncrement, NumberFieldInput, NumberFieldScrubArea } from '@pharen/ui';
</script>

<template>
  <NumberField :default-value="12" :min="0" :max="100" class="w-56">
    <NumberFieldScrubArea label="Retry limit" />
    <NumberFieldGroup>
      <NumberFieldDecrement />
      <NumberFieldInput />
      <NumberFieldIncrement />
    </NumberFieldGroup>
  </NumberField>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { NumberField, NumberFieldDecrement, NumberFieldGroup, NumberFieldIncrement, NumberFieldInput, NumberFieldScrubArea } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `NumberField` | Exported Vue component from this module. |
| `NumberFieldDecrement` | Exported Vue component from this module. |
| `NumberFieldGroup` | Groups related items. |
| `NumberFieldIncrement` | Exported Vue component from this module. |
| `NumberFieldInput` | Exported Vue component from this module. |
| `NumberFieldScrubArea` | Exported Vue component from this module. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/number-field)
