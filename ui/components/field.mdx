---
title: "Field"
sidebarTitle: "Field"
description: "Import and use the Field components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Field** to collect and edit a value. This page covers every Vue component exported by the public module.

<PharenUiPreview name="field" title="Field preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { Field, FieldDescription, FieldLabel, Input } from '@pharen/ui';
</script>

<template>
  <Field class="w-full max-w-sm">
    <FieldLabel for="workspace-name">Workspace name</FieldLabel>
    <Input id="workspace-name" value="Pharen" />
    <FieldDescription>This name is visible to every workspace member.</FieldDescription>
  </Field>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { Field, FieldContent, FieldDescription, FieldError, FieldGroup, FieldLabel, FieldLegend, FieldSeparator, FieldSet, FieldTitle } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `Field` | Exported Vue component from this module. |
| `FieldContent` | Contains the visible content. |
| `FieldDescription` | Renders supporting description. |
| `FieldError` | Exported Vue component from this module. |
| `FieldGroup` | Groups related items. |
| `FieldLabel` | Labels a value or region. |
| `FieldLegend` | Exported Vue component from this module. |
| `FieldSeparator` | Separates adjacent regions. |
| `FieldSet` | Exported Vue component from this module. |
| `FieldTitle` | Renders the accessible title. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/field)
