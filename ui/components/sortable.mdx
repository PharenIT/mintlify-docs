---
title: "Sortable"
sidebarTitle: "Sortable"
description: "Import and use the Sortable components from @pharen/ui."
---

import { PharenUiPreview } from "/snippets/pharen-ui-preview.jsx"

Use **Sortable** to reorder items with drag-and-drop interactions. This page covers every Vue component exported by the public module.

<PharenUiPreview name="sortable" title="Sortable preview" />

<Accordion title="View code">

```vue
<script setup lang="ts">
import { ref } from 'vue';
import { Sortable, SortableItem, SortableItemHandle } from '@pharen/ui';

const tasks = ref([
  { id: 'research', label: 'Research competitors' },
  { id: 'draft', label: 'Draft campaign' },
  { id: 'review', label: 'Review copy' },
]);
</script>

<template>
  <Sortable v-model="tasks" :get-item-value="item => item.id" class="grid w-full max-w-sm gap-2">
    <SortableItem v-for="task in tasks" :key="task.id" :value="task.id" class="flex items-center gap-2 rounded-lg border bg-card p-3">
      <SortableItemHandle :label="`Move ${task.label}`" />
      <span class="text-sm">{{ task.label }}</span>
    </SortableItem>
  </Sortable>
</template>
```

</Accordion>

## Installation

<CodeGroup>
```bash npm
npm install @pharen/ui
```

```bash pnpm
pnpm add @pharen/ui
```

```bash yarn
yarn add @pharen/ui
```

```bash bun
bun add @pharen/ui
```
</CodeGroup>

<Note>
  Import `@pharen/ui/styles.css` exactly once in your application entrypoint.
</Note>

## Usage

```ts
import { Sortable, SortableItem, SortableItemHandle, SortableOverlay } from '@pharen/ui';
```

## Exports

| Export | Purpose |
| --- | --- |
| `Sortable` | Exported Vue component from this module. |
| `SortableItem` | Represents one item. |
| `SortableItemHandle` | Exported Vue component from this module. |
| `SortableOverlay` | Exported Vue component from this module. |

## Source

[Open the source on GitHub](https://github.com/PharenIT/ELEV8-Labs/tree/main/src/packages/ui/src/components/sortable)
